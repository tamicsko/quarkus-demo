# =============================================================================
# Customer Service — közös (profil-független) konfiguráció
# =============================================================================

quarkus.application.name=customer-service
quarkus.http.port=8081

# Hibernate — Liquibase kezeli a sémát, nem Hibernate
quarkus.hibernate-orm.schema-management.strategy=none

# Liquibase
quarkus.liquibase.migrate-at-start=true
quarkus.liquibase.change-log=db/changeLog.xml
quarkus.liquibase.default-schema-name=customer_svc

# Logging
quarkus.log.category."dev.benno".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) [%X{correlationId}] %s%e%n
