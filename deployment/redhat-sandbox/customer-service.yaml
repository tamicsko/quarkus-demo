apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: tamicsko-dev
  labels:
    app: customer-service
    app.kubernetes.io/part-of: bank-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
        - name: customer-service
          image: image-registry.openshift-image-registry.svc:5000/tamicsko-dev/customer-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: QUARKUS_PROFILE
              value: container
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: service-config
                  key: DB_HOST
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: service-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: customer-svc-db
                  key: password
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: tamicsko-dev
spec:
  selector:
    app: customer-service
  ports:
    - port: 8081
      targetPort: 8081
